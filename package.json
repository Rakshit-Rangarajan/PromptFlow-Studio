{
  "name": "promptflow-studio",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "start /B cmd /c \"cd backend && .venv\\Scripts\\python.exe -m uvicorn app.main:app --host 127.0.0.1 --port 8000 --reload\" & npm.cmd --prefix frontend run dev",
    "build": "cd frontend && npm install && npm run build",
    "preview": "npm --prefix frontend run preview"
  }
}
